## Summary

What this change does and why.

## Related

Closes #… / relates to a `ROADMAP.md` or `TODO.md` item.

## How I verified

- [ ] `go test ./... -race` passes
- [ ] `gofmt -s` clean, `golangci-lint run` clean
- [ ] `tsc -b` + `npm run build` pass (if frontend touched)
- [ ] Manually exercised the change (describe how)

## Notes for reviewers

Anything tricky, follow-ups, or deliberate trade-offs. Docs updated (`ARCHITECTURE.md` / `CLAUDE.md` / `README.md`) where behavior or conventions changed.
